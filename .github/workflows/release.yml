---
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions: read-all

jobs:
  release:
    uses: "Mosher-Labs/.github/.github/workflows/release.yml@main"
    permissions:
      contents: write
      issues: write
      pull-requests: write
    secrets: inherit
